name: Test

on: pull_request

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: cat $GITHUB_EVENT_PATH
      - run: echo ${{contains(github.event.pull_request.labels.*.name, 'duplicate')}}
