name: Test

on:
  push:
    branches:
      - master

jobs:
  test:
    runs-on: macos-latest
    steps:
      - run: which -a svn
      - run: |
          svn --version
          /usr/bin/svn --version
      - run: brew ruby -e "p Utils.svn_available?"
