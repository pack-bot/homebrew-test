name: Composite Action
description: Test
inputs:
  test:
    description: Test
    default: test
outputs:
  test: 
    description: Test
    value: ${{steps.test.outputs.test}}
runs:
    using: composite
    steps:
      - run: echo test1
        shell: bash
      - run: echo test2
        shell: bash
